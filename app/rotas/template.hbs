{{#swipe-container
   collection=attrs.rotaWeeks
   tabPropertyKey="formattedDateRange"
   selectedIndex=selectedIndex
   setSelectedIndex="setSelectedIndex"
   myClass="-rota"
   isPanning=isPanning
}}
  {{#each attrs.rotaWeeks as |rotaWeek index|}}
    {{#swipe-item index=index selectedIndex=selectedIndex}}
      <div class="card">
        {{rota-week
         week=rotaWeek
         shifts=rotaWeek.shifts
         selectedShift=selectedShift
         selectTarget=this
         index=index
         selectedIndex=selectedIndex
        }}
      </div>
    {{/swipe-item}}
  {{/each}}
{{/swipe-container}}
{{holiday-info info=holiday}}
